  <PropertyGroup Label="Globals">
    <Configuration Condition="$(Configuration) == ''">Release</Configuration>
    <Platform Condition="$(Platform) == ''">x64</Platform>
    <ProjectGuid>{{{guid!s}}}</ProjectGuid>
    <RootNamespace>{project.target_name}</RootNamespace>
    <TargetName>{project.target_name}</TargetName>
    <VersionString>{distinfo[version]}</VersionString>
  </PropertyGroup>
  <Target Name="_VerifyProperties" BeforeTargets="Build">
    <Error Text="OutDir property is required" Condition="$(OutDir) == ''" />
    <Error Text="IntDir property is required" Condition="$(IntDir) == ''" />
  </Target>