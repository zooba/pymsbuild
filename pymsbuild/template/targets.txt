  <Target Name="BuildDependencies">
    <MSBuild
        Projects="@(Project)"
        Targets="Build"
        Properties="OutDirRoot=%(Destination)">
      <Output TaskParameter="TargetOutputs" ItemName="_Outputs" />
    </MSBuild>
  </Target>

  <Target Name="CopyContent" Inputs="@(Content)" Outputs="%(Content.Destination)">
    <Message Text="%(Content.FullPath) --> %(Content.Destination)\%(Content.FileName)%(Content.Extension)" Importance="high" />
  </Target>

  <Target Name="GetTargetPath" Outputs="@(Content)" />

  <Target Name="Build" DependsOnTargets="BuildDependencies;CopyContent">
  </Target>

  <Target Name="BuildSdist" DependsOnTargets="">
  </Target>

  <Target Name="Clean">
    <MSBuild Projects="@(Project)"
             Targets="Clean" />
  </Target>

  <Target Name="Rebuild" DependsOnTargets="Clean;Build" />
